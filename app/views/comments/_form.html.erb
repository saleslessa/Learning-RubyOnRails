<%= form_for([@article, @article.comments.build]) do |f| %>
  <p>
    <%= f.label :comenter %><br>
    <%= f.text_field :comenter %>
  </p>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>